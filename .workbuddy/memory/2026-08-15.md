# 2026-08-15

## 为 Adammoonlight.github.io 搭建纯前端薪资计算器 + CI/CD
- 仓库原本是空的 Hexo 博客脚手架（0 字节占位页、默认 content.json/search.xml），与简介"个税+五险一金到手薪资计算器"不符。已清理这些 Hexo 遗留文件。
- 新增应用：`index.html` / `styles.css` / `app.js`（纯静态 UI，无后端）+ `calc.js`（个税月度税率表 + 五险一金个人缴纳，浏览器/Node 双兼容）+ `calc.test.js`（Node 内置 test runner，10 个用例全过）。
- 新增 GitHub Actions：
  - `.github/workflows/ci.yml`：PR/推送 master 时 `node --check` 语法校验 + `npm test` 单元测试。
  - `.github/workflows/deploy.yml`：推送 master 时跑测试 → 构建 dist → `actions/deploy-pages` 部署 GitHub Pages（需仓库 Settings→Pages→Source 设为 "GitHub Actions"）。
- 个税口径：按月预扣月度税率表（3%/10%/20%/25%/30%/35%/45%，速扣 0/210/1410/2660/4410/7160/15160）。五险一金默认 养老8% 医疗2% 失业0.5% 公积金12%。
- 已本地提交（aac21b8），但 push 被阻断：本机无 GitHub 凭据（github 连接器未连接、无 gh、HTTPS 禁用交互提示）。待鱼哥手动 `git push` 或连接 GitHub 连接器后推送。
